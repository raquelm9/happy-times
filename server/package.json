{
  "name": "happy-times-server",
  "version": "1.0.0",
  "description": "Find discounts and happy hours in your local area",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "npm run build && node ./src/server.js",
    "start:watch": "nodemon ./src/server.js",
    "clean": "rm -rf build",
    "build": "babel src -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "lodash": "^4.17.20",
    "mongoose": "^5.11.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0"
  },
  "engines": {
    "node": "10.x"
  }
}
